{
  "name": "niem-xml-to-js",
  "version": "1.0.1",
  "description": "Convert a NIEM XML instance to NIEM JSON, a NIEM JSON template (data values replaced with empty strings and other default values), and a JSON Schema generated from the instance.",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "to-json-schema": "^0.2.5",
    "xml2js": "^0.4.23"
  },
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "coveralls": "^3.1.1",
    "jest": "^27.4.7"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
